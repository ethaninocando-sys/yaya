import { INSTAGRAM_HANDLE } from "@/lib/utils";

export default function AnnouncementBar() {
  return (
    <div className="bg-boutique-brown px-4 py-[9px] text-center text-[12.5px] font-semibold uppercase tracking-[0.18em] text-[#e8d9be]">
      Book your Hat Bar session — In-Store or Virtual · DM {INSTAGRAM_HANDLE}
    </div>
  );
}
